# Exclude node_modules so Docker builds clean dependencies inside the container
node_modules

# Exclude local Next.js build artifacts and logs
.next
out
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Exclude environment secrets and git tracking
.env
.env.*
.env.local
!.env.example
!.env.*.example
.git
.gitignore
README.md
